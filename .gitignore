# macOS
.DS_Store

# Editor / sync leftovers (these appear when a file is open in a synced folder)
.fuse_hidden*
*~
*.swp

# Vercel CLI local link
.vercel

# Nothing here should ever need npm — but just in case
node_modules/

# Local Claude Code permissions (machine-specific)
.claude/settings.local.json
